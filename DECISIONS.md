# 결정 기록

결정 상태는 `제안`, `확정`, `보류`, `폐기`, `대체됨`으로 관리한다. 확정된 결정을 바꿀 때는 기존 내용을 삭제하지 않고 새 결정으로 대체한다.

## 확정된 결정

| ID | 결정 | 이유 |
|---|---|---|
| D-001 | 제품의 중심은 프로젝트별 실제 토큰 추적이다. | 한도 표시는 부가 기능이다. |
| D-002 | 기기 수는 고정하지 않고 각 기기에 UUID를 부여한다. | 집·회사 두 대로 제한하지 않는다. |
| D-003 | 같은 정규화 Git remote는 같은 프로젝트로 본다. | 로컬 폴더명과 Codex 작업명이 달라도 통합한다. |
| D-004 | GitHub 비공개 저장소를 기기 간 공용 장부로 사용한다. | 개인용 동기화와 기록 보존이 목적이다. |
| D-005 | 원본 대화·코드·명령 출력은 GitHub에 올리지 않는다. | 개인정보와 회사 정보를 보호한다. |
| D-006 | 중앙 장부의 원본 단위는 정제된 토큰 사용 이벤트다. | 날짜·모델·작업별로 다시 집계할 수 있어야 한다. |
| D-007 | 이벤트에 누적 체크포인트와 계산된 증가량을 함께 보존한다. | 파서 오류가 발견돼도 장부에서 재계산할 수 있다. |
| D-008 | 날짜별·주간·누적 사용량은 이벤트에서 계산한다. | 집계값을 원본으로 두지 않는다. |
| D-009 | 프로젝트에 연결되는 작업 수는 제한하지 않는다. | 오케스트레이터와 여러 일반 작업을 모두 포함한다. |
| D-010 | 프로젝트는 오케스트레이터와 자식·CLI·백그라운드 작업의 사용량을 포함한다. | 멀티에이전트 사용량이 흩어지는 문제를 해결한다. |
| D-011 | 캐시와 추론 토큰을 총 토큰에 다시 더하지 않는다. | 세부 지표의 중복 합산을 막는다. |
| D-012 | SQLite 파일 자체는 GitHub에서 공유하지 않는다. | 바이너리 충돌을 피하고 장부에서 재생성한다. |
| D-013 | Codex 여백 UI는 TODO로 보류한다. | 데이터 수집과 정확성을 먼저 확정한다. |
| D-014 | 단계가 바뀔 때 사용자 승인을 받는다. | 기획과 구현을 임의로 진행하지 않는다. |

## 제안된 결정

| ID | 제안 | 상태 |
|---|---|---|
| D-015 | 프로젝트 판별 우선순위는 `수동 지정 → 자기 Git 정보 → 가장 가까운 부모 상속 → 미분류`로 한다. | Spike 3에서 turn 활동 위치가 빠진 것으로 확인, D-022가 대체 제안 |
| D-016 | 이벤트 시각은 UTC로 저장하고 기본 날짜 표시는 Asia/Seoul로 한다. | 사용자 승인 대기 |
| D-017 | GitHub 장부는 기기별 디렉터리와 append-only JSONL을 사용한다. | 상세 스키마 승인 대기 |
| D-018 | 프로젝트 별칭과 수동 연결도 기기별 append-only 매핑 이벤트로 기록한다. | 충돌 해결 규칙 승인 대기 |
| D-019 | 계정 한도는 Codex가 제공한 값만 저장하고 토큰에서 역산하지 않는다. | 사용자 승인 대기 |
| D-020 | fork 중복 제거의 멱등 키 후보로 `turn_id + turn 내부 token 이벤트 순번`을 사용한다. | Spike 2 근거 확보, 다기기 검증 후 승인 대기 |
| D-021 | compact의 불투명한 `last_token_usage`는 원형을 보존하되 일반 프로젝트 합계에는 즉시 더하지 않는다. | 실제 의미와 한도 반영 여부 확인 후 승인 대기 |
| D-022 | 프로젝트 귀속은 usage event가 속한 turn 단위로 결정하고 `수동 → 단일 활동 Git → 자기 Git → 계보 → 미분류` 순서를 사용한다. | Spike 3 근거 확보, 사용자 승인 대기 |
| D-023 | foreground와 background `codex exec`를 별도 프로젝트 종류로 나누지 않는다. | Spike 3 근거 확보, 사용자 승인 대기 |
| D-024 | 도구 실행 경로는 로컬에서만 Git project ID로 변환하고 원문 경로·명령은 중앙 장부에 저장하지 않는다. | Spike 3 근거 확보, 사용자 승인 대기 |
| D-025 | origin이 없으면 로컬 remote가 정확히 하나일 때만 그 URL을 사용하고, 0개는 local-only, 2개 이상은 ambiguous로 둔다. | Spike 4 근거 확보, 사용자 승인 대기 |
| D-026 | worktree는 같은 remote 프로젝트로 합치고, submodule은 자기 remote를 우선하며, monorepo는 기본적으로 저장소 하나로 본다. | Spike 4 근거 확보, 사용자 승인 대기 |
| D-027 | remote 변경은 과거 이벤트 수정이 아니라 append-only project alias로 연결한다. | Spike 4 근거 확보, 사용자 승인 대기 |

## 보류된 결정

| ID | 결정이 필요한 내용 |
|---|---|
| P-001 | 회사 기기의 정제 통계를 개인 GitHub에 저장할지 |
| P-002 | remote·branch·경로 정보를 원문, HMAC, 별도 ID 중 어떤 형태로 저장할지 |
| P-003 | 수집기를 수동, 주기 실행, Codex 종료 시 실행 중 어떤 방식으로 동작시킬지 |
| P-004 | v1 기본 조회 방식을 CLI, Markdown 보고서, 로컬 화면 중 무엇으로 할지 |
| P-005 | Spike 2 후보인 `turn_id + token 이벤트 순번`을 다기기·구버전에서도 사용할지 |
| P-006 | 파서 오류 정정 시 기존 이벤트를 어떻게 대체할지 |
| P-007 | 부모 프로젝트와 자식 Git 저장소가 다를 때 어느 쪽으로 귀속할지 |
| P-008 | monorepo를 저장소 하나로 볼지 하위 프로젝트로 나눌지 |
| P-009 | 이벤트 장부의 롤업·아카이브·보존 기간을 어떻게 정할지 |
| P-010 | Spike 2에서 확인한 turn 단위 중복 제거 방식을 최종 확정할지 |
| P-011 | compact의 불투명한 reported last를 별도 overhead 통계로 표시할지 |
| P-012 | 한 turn이 여러 저장소를 사용하면 `ambiguous_multi_repo`로 둘지 |
| P-013 | D-022의 turn별 활동 Git 우선순위를 최종 확정할지 |
| P-014 | remote 없는 저장소를 여러 기기에서 연결할 project marker를 저장소 안에 둘지 |
| P-015 | 정규화 remote에서 project ID를 만드는 비식별화 방식을 HMAC으로 확정할지 |

## 대체된 결정

| 이전 결정 | 대체 결정 |
|---|---|
| 집 사용량 + 회사 사용량으로 고정 | 기기 UUID를 가진 임의 개수 기기의 합 |
| 로컬 폴더명으로 프로젝트 판별 | 정규화 Git remote와 계보·매핑으로 판별 |
| 날짜별 합계를 원본으로 저장 | 정제 사용 이벤트를 원본으로 저장 |
| 누적 토큰값을 단순 합산 | 누적 체크포인트 차이로 증가량 계산 |
| 한도 모니터 중심 UI | 프로젝트 실제 토큰 분석 중심 |
| 프로젝트 위에 별도 작업 그룹 추가 | 프로젝트 안에 오케스트레이션 계보 포함 |
