# 결정 기록

결정 상태는 `제안`, `확정`, `보류`, `폐기`, `대체됨`으로 관리한다. 확정된 결정을 바꿀 때는 기존 내용을 삭제하지 않고 새 결정으로 대체한다.

## 확정된 결정

| ID | 결정 | 이유 |
|---|---|---|
| D-001 | 제품의 중심은 프로젝트별 실제 토큰 추적이다. | 한도 표시는 부가 기능이다. |
| D-002 | 기기 수는 고정하지 않고 각 기기에 UUID를 부여한다. | 집·회사 두 대로 제한하지 않는다. |
| D-003 | 같은 정규화 Git remote는 같은 프로젝트로 본다. | 로컬 폴더명과 Codex 작업명이 달라도 통합한다. |
| D-004 | GitHub 비공개 저장소를 기기 간 공용 장부로 사용한다. | 개인용 동기화와 기록 보존이 목적이다. |
| D-005 | 원본 대화·코드·명령 출력은 GitHub에 올리지 않는다. | 개인정보와 회사 정보를 보호한다. |
| D-006 | 중앙 장부의 원본 단위는 정제된 토큰 사용 이벤트다. | 날짜·모델·작업별로 다시 집계할 수 있어야 한다. |
| D-007 | 이벤트에 누적 체크포인트와 계산된 증가량을 함께 보존한다. | 파서 오류가 발견돼도 장부에서 재계산할 수 있다. |
| D-008 | 날짜별·주간·누적 사용량은 이벤트에서 계산한다. | 집계값을 원본으로 두지 않는다. |
| D-009 | 프로젝트에 연결되는 작업 수는 제한하지 않는다. | 오케스트레이터와 여러 일반 작업을 모두 포함한다. |
| D-010 | 프로젝트는 오케스트레이터와 자식·CLI·백그라운드 작업의 사용량을 포함한다. | 멀티에이전트 사용량이 흩어지는 문제를 해결한다. |
| D-011 | 캐시와 추론 토큰을 총 토큰에 다시 더하지 않는다. | 세부 지표의 중복 합산을 막는다. |
| D-012 | SQLite 파일 자체는 GitHub에서 공유하지 않는다. | 바이너리 충돌을 피하고 장부에서 재생성한다. |
| D-013 | Codex 여백 UI는 TODO로 보류한다. | 데이터 수집과 정확성을 먼저 확정한다. |
| D-014 | 단계가 바뀔 때 사용자 승인을 받는다. | 기획과 구현을 임의로 진행하지 않는다. |
| D-016 | 이벤트 시각은 UTC로 저장하고 기본 날짜 표시는 Asia/Seoul로 한다. | 기기별 시간대를 통일하면서 사용자는 한국 날짜로 조회한다. |
| D-017 | GitHub 장부는 기기별 디렉터리와 append-only JSONL을 사용한다. | 여러 기기의 동시 쓰기 충돌을 줄인다. |
| D-018 | 프로젝트 별칭과 수동 연결도 기기별 append-only 매핑 이벤트로 기록한다. | 공용 파일 직접 수정과 이력 손실을 피한다. |
| D-019 | 계정 한도는 Codex가 제공한 값만 저장하고 토큰에서 역산하지 않는다. | 실제 토큰과 구독 한도 소모율은 다른 지표다. |
| D-020 | usage 이벤트의 기본 멱등 키는 `turn_id + turn 내부 token 이벤트 순번`으로 만든다. | fork가 복사한 동일 turn을 중복 제거한다. |
| D-021 | compact의 불투명한 `last_token_usage`는 별도 보존하고 일반 프로젝트 총합에서는 제외한다. | 의미가 검증되지 않은 값을 실제 사용량에 섞지 않는다. |
| D-022 | 프로젝트 귀속은 turn 단위로 결정하고 `수동 → 단일 활동 Git → 자기 Git → 계보 → 미분류` 순서를 사용한다. | 프로젝트 밖 오케스트레이션도 실제 활동 위치로 분류한다. |
| D-023 | foreground와 background `codex exec`를 별도 프로젝트 종류로 나누지 않는다. | 둘 다 `source=exec`으로 기록된다. |
| D-024 | 도구 실행 경로는 로컬에서만 project ID로 변환하고 원문 경로·명령은 중앙 장부에 저장하지 않는다. | 자동 귀속과 개인정보 보호를 함께 만족한다. |
| D-025 | origin이 없으면 로컬 remote가 정확히 하나일 때만 그 URL을 사용하고, 0개는 local-only, 2개 이상은 ambiguous로 둔다. | 임의 remote 선택으로 잘못 합치는 것을 막는다. |
| D-026 | worktree는 같은 remote 프로젝트로 합치고, submodule은 자기 remote를 우선하며, monorepo는 기본적으로 저장소 하나로 본다. | Spike 4의 실제 Git 메타데이터 결과를 따른다. |
| D-027 | remote 변경은 과거 이벤트 수정이 아니라 append-only project alias로 연결한다. | 장부의 불변성과 저장소 이전 이력을 보존한다. |
| D-028 | 회사 데이터는 회사 정책이 허용할 때 정제 통계만 개인 비공개 장부에 저장한다. | 정책 위반 가능성을 사용자 책임 하에 명시적으로 제한한다. |
| D-029 | project·thread·turn 식별자는 기기 간 공유 비밀키로 HMAC 처리하고 키는 Git에 저장하지 않는다. | 여러 기기에서 같은 대상을 합치면서 원문 식별자를 숨긴다. |
| D-030 | v1 수집 흐름은 수동 CLI `collect → sync → report`로 제공하고 자동 실행은 후순위로 둔다. | 정확성과 복구 가능성을 먼저 검증한다. |
| D-031 | v1 기본 조회는 CLI 표와 Markdown 보고서다. | 문서화 목적을 충족하면서 UI 개발을 미룬다. |
| D-032 | 파서 정정은 기존 이벤트를 삭제하지 않고 새 revision과 `supersedes`로 대체한다. | append-only 장부에서 수정 이력을 보존한다. |
| D-033 | v1에서는 이벤트를 삭제·롤업하지 않고 보존한다. | 개인용 규모에서는 단순성과 재계산 가능성이 우선이다. |
| D-034 | remote 없는 저장소의 다기기 연결은 수동으로 같은 project ID에 연결한다. | 불안정한 commit·경로 기반 자동 추론을 피한다. |
| D-035 | 한 turn에서 여러 저장소가 확인되면 `ambiguous_multi_repo`로 저장하고 수동 지정한다. | 토큰을 저장소별로 정확히 분할할 근거가 없다. |
| D-036 | v1은 Python 3.12+ modular monolith CLI로 구현한다. | Windows 로컬 데이터 처리와 빠른 parser 반복 개발에 적합하다. |
| D-037 | JSONL은 상세 token 원천, SQLite는 inventory·lineage 원천으로 사용하는 이중 adapter 구조를 사용한다. | 두 원천의 장점을 결합한다. |
| D-038 | 로컬 SQLite outbox와 재생성 가능한 read model을 함께 사용한다. | crash-safe 수집과 빠른 조회를 함께 만족한다. |
| D-039 | HMAC 키는 Windows Credential Manager에 저장하고 비가역 key ID로 기기 불일치를 탐지한다. | 비밀을 Git에 두지 않고 잘못된 키 혼용을 차단한다. |
| D-040 | Git sync conflict·privacy failure·key mismatch는 자동 해결하지 않고 안전하게 중단한다. | 데이터 손상과 원문 유출을 방지한다. |
| D-041 | unchanged rollout은 fingerprint로 건너뛰고 changed rollout은 처음부터 재계산한 뒤 기존 source event를 제외한다. | 누적 token 기준선 정확성과 반복 수집 성능을 함께 유지한다. |
| D-042 | 계속 쓰이는 rollout과 손상된 rollout은 cursor를 전진시키지 않고 파일 단위로 격리한다. | 한 파일 때문에 전체 수집을 중단하지 않으면서 손상 데이터를 합계에 넣지 않는다. |
| D-043 | 첫 기기는 복구 키를 한 번 표시하고, 추가 기기는 숨김 입력으로 가져와 Windows Credential Manager에 저장한다. | 서버 없이 여러 기기가 같은 HMAC ID 공간을 사용해야 한다. |
| D-044 | 보고서는 UTC 원본을 Asia/Seoul 날짜로 변환하고 date 외 그룹 차원별 누적합을 계산한다. | 여러 기기의 시각을 통일하면서 사용자가 실제 생활 날짜와 프로젝트 흐름을 읽을 수 있게 한다. |
| D-045 | delta가 null인 이벤트는 보고서 합계에서 제외하되 제외 건수로 표시하고, 부분 세부 필드는 `*`로 표시한다. | 불확실한 값을 0으로 위장하지 않고 알려진 합계와 데이터 품질을 함께 보여준다. |
| D-046 | Git sync는 자기 device 경로의 append-only JSONL만 커밋하고, fetch·rebase 후 전체 장부를 다시 검증·replay한 뒤 fast-forward push한다. 충돌과 정책 위반은 자동 해결하지 않으며 실패한 로컬 커밋은 보존한다. | 여러 기기의 동시 변경을 합치면서 장부 변조·민감정보 유출·사용량 손실을 막는다. |
| D-047 | 구독 한도와 초기화 시각 수집은 v1 핵심 프로그램 구현과 수용 검증이 끝난 뒤 추가한다. | 프로젝트별 실제 토큰 추적과 수동 분류·진단 완성을 우선한다. |

## 제안된 결정

현재 제안 상태로 남은 결정은 없다.

## 해결된 보류 항목

| 이전 ID | 해결 결정 |
|---|---|
| P-001 | D-028 |
| P-002, P-015 | D-024, D-029 |
| P-003 | D-030 |
| P-004 | D-031 |
| P-005, P-010 | D-020 |
| P-006 | D-032 |
| P-007, P-013 | D-022 |
| P-008 | D-026 |
| P-009 | D-033 |
| P-011 | D-021 |
| P-012 | D-035 |
| P-014 | D-034 |

## 대체된 결정

| 이전 결정 | 대체 결정 |
|---|---|
| 집 사용량 + 회사 사용량으로 고정 | 기기 UUID를 가진 임의 개수 기기의 합 |
| 로컬 폴더명으로 프로젝트 판별 | 정규화 Git remote와 계보·매핑으로 판별 |
| 날짜별 합계를 원본으로 저장 | 정제 사용 이벤트를 원본으로 저장 |
| 누적 토큰값을 단순 합산 | 누적 체크포인트 차이로 증가량 계산 |
| 한도 모니터 중심 UI | 프로젝트 실제 토큰 분석 중심 |
| 프로젝트 위에 별도 작업 그룹 추가 | 프로젝트 안에 오케스트레이션 계보 포함 |
| D-015의 thread 중심 귀속 순서 | D-022의 turn 활동 중심 귀속 순서 |
